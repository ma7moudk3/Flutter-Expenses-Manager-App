import 'package:flutter/material.dart';

class CreateExpenseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('CreateExpenseScreen'),
    );
  }
}
