import 'package:flutter/material.dart';

class StaticsesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Staticses Screen'),
    );
  }
}
